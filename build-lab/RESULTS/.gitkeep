Evidence directory. One subdirectory per lab build, one `ab-*` subdirectory per
A/B session. Committing results is the point — they are the documented proof
that a build-system change was safe, and they are worth more later than now.
