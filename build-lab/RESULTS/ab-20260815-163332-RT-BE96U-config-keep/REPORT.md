# A/B result — config-keep — RT-BE96U

- session: `ab-20260815-163332-RT-BE96U-config-keep`
- date (UTC): 2026-08-15T21:12:31Z
- tree: `/home/reaper/asuswrt-be96u` @ `7428362b78` (`be96u-only`)
- version: `Reaper_v2.4.4`
- variants: `MCP noMCP`  ·  replicates: 1  ·  warm-up: yes  ·  order: `BA`
- ordering note: the candidate ran **first**, on the colder tree, so any win here is conservative.
- arm A (baseline): `--single-pass auto`
- arm B (candidate): `--single-pass auto --config-keep`

## Timing

| arm | replicate | rc | total (s) | total (m:ss) |
|---|---|---|---|---|
| B | 1 | 0 | 781 | 13:01 |
| A | 1 | 0 | 779 | 12:59 |

## Content equality (the criterion that decides promotion)

Digests are **normalised** — the paths in `nondeterministic.txt` are excluded,
because this build is not bit-reproducible and never has been. The raw digest is
shown too; a future reproducible-builds effort would drive the two together.

| variant | arm | replicate | normalised digest | raw digest | verify P/W/F |
|---|---|---|---|---|---|
| MCP | A | 1 | `9e7e1efa9b627fd20e070a42a4e08adb3ac0172a2531170942bb0cec1d2c30ea` | `d1c811aac7952009` | 20/0/0 |
| MCP | B | 1 | `9e7e1efa9b627fd20e070a42a4e08adb3ac0172a2531170942bb0cec1d2c30ea` | `623f7b32e377195c` | 20/0/0 |
| noMCP | A | 1 | `1348328ff0be7155b686dcecba19298b2c1ff0e55edd052dd8f7103bcc4009c6` | `7beeedbf808edfff` | 20/0/0 |
| noMCP | B | 1 | `1348328ff0be7155b686dcecba19298b2c1ff0e55edd052dd8f7103bcc4009c6` | `d0d806b87bfaa780` | 20/0/0 |

### Irreproducibility noise floor

Files differing between the warm-up and arm A — two builds of **identical**
configuration. Anything the candidate changes beyond this set is real.

`MCP` — noise floor: 5 files · candidate: 5 files

```
noise floor (A-config vs A-config):
  ./lib/modules/4.19.294/modules.dep
  ./rom/etc/build_time
  ./rom/etc/image_version
  ./rom/etc/motd
  ./usr/lib/libshared.so
candidate   (A vs B):
  ./lib/modules/4.19.294/modules.dep
  ./rom/etc/build_time
  ./rom/etc/image_version
  ./rom/etc/motd
  ./usr/lib/libshared.so
beyond the noise floor (REAL divergence if non-empty):
```

`noMCP` — noise floor: 5 files · candidate: 5 files

```
noise floor (A-config vs A-config):
  ./lib/modules/4.19.294/modules.dep
  ./rom/etc/build_time
  ./rom/etc/image_version
  ./rom/etc/motd
  ./usr/lib/libshared.so
candidate   (A vs B):
  ./lib/modules/4.19.294/modules.dep
  ./rom/etc/build_time
  ./rom/etc/image_version
  ./rom/etc/motd
  ./usr/lib/libshared.so
beyond the noise floor (REAL divergence if non-empty):
```

## Verdict

- **PASS — content equivalent.** Normalised digests match across every arm and replicate, and the candidate changed nothing that two identical-config builds do not also change.
- **PASS — reaper_verify clean in every run.**
- replicate 1: 779s → 781s (**no improvement**)
- aggregate: 779s → 781s over 1 replicates

### DO NOT PROMOTE

At least one criterion failed. The evidence is in this directory; the
per-run build logs carry the `[lab-timing]` and `[lab-decision]` lines
that explain what each arm actually did.

---

_Generated by `build-lab/ab_compare.sh`. No file outside `build-lab/RESULTS/` was modified._
