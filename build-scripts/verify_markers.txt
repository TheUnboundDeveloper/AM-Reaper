# reaper_verify patch-marker manifest -- proves rung-critical fixes are
# physically inside every staged image, per model, per variant.
# Format:  <staged-path>|<literal string>|<min-count>     (>= min occurrences)
#          <staged-path>|!<literal string>                (must be ABSENT)
# Add a line whenever a field-critical fix ships; prune only when obsolete.
#
# v2.1.5/v2.1.7 -- PPPoE baby-jumbo MTU 1500 in the SHIPPED WAN page.
# Model nuance (found by this very check, 2026-08-05): GT-BE98 (non-Pro) builds
# WITHOUT MULTISERVICE_WAN (its target.mak block never had the token - separate
# GPL lineage), so it installs the BASE WAN page = 2x "128, 1500"; the MSWAN
# variant on the other four models has 4x. min=2 covers both; the forbidden
# 1492 line is the real tripwire either way.
www/Advanced_WAN_Content.asp|128, 1500|2
www/Advanced_WAN_Content.asp|!128, 1492
# v2.1.8 -- the 1508 WAN-MTU allowance is ROLLED BACK (owner field report: the
# visible WAN-MTU box only shows on DHCP/static where 1508 is invalid; 1508 was
# enterable + applyable). wan_mtu caps at 1500 again; PPPoE keeps 1500 with the
# automatic backend +8. A 1508-capped page must never ship again:
www/Advanced_WAN_Content.asp|!1280, 1508
# v2.1.7 -- rename popup re-reads live custom_clientlist at save:
www/client_function.js|nvramCharToAscii(["custom_clientlist"]|1
# v2.1.7 -- DHCP leases page carries the unified Device Name column:
www/Main_DHCPStatus_Content.asp|Device Name|1
www/Main_DHCPStatus_Content.asp|fetchDeviceNames|2
# 2026-08-06 -- Gatekeeper internet-only DNS carve-out (owner field report: a
# DHCP-advertised LAN DNS (AdGuard) was sealed off by the bridge DROP, so an
# internet-only device had no resolver = no usable internet). The generator's
# shell-helper name proves the carve-out is compiled into rc:
sbin/rc|gk_same_net|1
# v2.9.1 -- the first-boot WIZARD AND ALL SIX OF ITS GATES ARE GONE. The two
# markers that used to live here guarded Reaper_FirstBoot.asp's exit target;
# the page no longer ships. What replaces them guards the opposite property:
# that nothing blocks a page any more, and that the banner which replaced the
# blocking is actually present in the image.
#
# The wizard must never come back, in any form:
www/Reaper_SecBanner.asp|rsec_state|1
usr/sbin/httpd|Reaper_SecBanner.asp|1
usr/sbin/httpd|!Reaper_FirstBoot.asp
usr/sbin/httpd|!reaper_fbdone
# The banner must carry all twelve translated strings, and the fragment must
# never carry the loader (it would fetch itself forever):
www/Reaper_SecBanner.asp|rsec_t_wifi_cta|1
www/Reaper_SecBanner.asp|!XMLHttpRequest
# It must NEVER be served pre-auth: the login page must not name the banner.
www/Main_Login.asp|!rsec
# The factory Wi-Fi key is now a real key on every band (2.4/5 psk2, 6 sae) -
# a shipped image that reverts to an OPEN factory network is the regression
# this whole change exists to remove:
usr/sbin/httpd|reaper_wifi_default|1
# 2026-08-06 -- Warden block count now persists across reboot/reflash: the
# cumulative baseline lives on /jffs (was /tmp, which cleared on reboot).
sbin/rc|/jffs/rwarden/blocked_base|1
# 2026-08-06 -- de-cloud: the stock TrendMicro "Security Update" auto-upgrade
# section is removed from the firmware page (must never ship again).
www/Advanced_FirmwareUpgrade_Content.asp|!secur_stab_setting
# v2.6.2 -- shared REAPER_HOOK front chains (fixed layer order Warden ->
# Gatekeeper -> rules engine): the generator must be compiled into rc, and
# gkd must assert the front-chain jump, not just chain existence (gkd is
# installed at bin/gkd - see gkd/Makefile).
sbin/rc|REAPER_HOOK_F|1
bin/gkd|REAPER_HOOK_F|1
# v2.6.2 -- Gatekeeper multi-network: the per-network record splitter proves
# the L3 leg + per-network DNS carve-out generator is in rc.
sbin/rc|gk_net_fields|1
# v2.6.3 -- Gatekeeper device list lives on /jffs (nvram capped it at 1 KB =
# 45 devices, silently). The store helper is in libshared; rc/httpd/gkd link it.
usr/lib/libshared.so|/jffs/gatekeeper/rl|1
# v2.6.7: policy routing commit-confirm (rollback source on /jffs) + its CGI +
# the WireGuard flow-cache bypass record. If any of these is missing the build
# silently shipped the v2.6.x MVP engine (WGC rules skipped, no auto-revert).
sbin/rc|/jffs/reaper_pbr/lastgood|1
sbin/rc|/tmp/reaper_pbr/skipnets|1
usr/sbin/httpd|reaper_pbr.cgi|1
www/Reaper_VPNRouting.asp|reaper_pbr.cgi|1
usr/lib/libshared.so|/jffs/reaper_pbr/lastgood/rulelist|1
# v2.6.9 -- the firewall's eight lists left nvram too (the 40-domain object
# that silently never saved); the resolver lives in libshared.
usr/lib/libshared.so|/tmp/reaper_fw/draft|1
sbin/rc|/tmp/reaper_pbr/expect|1
usr/sbin/httpd|reaper_cfg.cgi|1
# moved 2026-08-29 to the injected card; v2.9.8 absorbed into the native page
www/Reaper_Backup.asp|rbk_cx|1
# v2.7.1 security review: per-boot http_id (rc) + same-host Referer check on
# every Reaper CGI (httpd). Missing = the factory-constant CSRF token is back.
sbin/rc|/tmp/.http_id_boot|1
usr/sbin/httpd|cross-site request refused|1
# v2.7.2 -- Traffic Analyzer restore-proof history (rtrafd aligns its ring
# cursors to the db's own stamp; missing = the NTP step wipes restored
# month/day slots again) + the one-file full backup/restore (httpd archive
# handlers + the injected Administration card fragment) + the diag stamp.
# NOTE: the diag marker is VERSION-PINNED -- bump it in the same commit as
# any patch that bumps VER in reaper_diag, or every model fails patch-marker.
bin/rtrafd|ring cursors aligned to db stamp|1
usr/sbin/httpd|REAPER-BACKUP 1|1
usr/sbin/httpd|reaper_restore.cgi|1
www/Reaper_Backup.asp|rbk_dl|1
usr/sbin/reaper_diag|REAPER-DIAG v1.3.13|1
# v2.7.3 -- Gatekeeper learned AiMesh exists: cfg_relist infrastructure
# exemptions (rc) + gkd's onboarding window. Missing = quarantine breaks
# node onboarding and mesh heartbeats again.
sbin/rc|AiMesh infrastructure (cfg_relist)|1
bin/gkd|AiMesh onboarding active|1
# v2.7.3 -- signed update manifest (fail-closed verify in the check script)
# + rwatch heals under the firewall fcntl lock via the rc applet.
usr/sbin/webs_update.sh|manifest signature|1
usr/sbin/webs_update.sh|BEGIN PUBLIC KEY|1
sbin/rc|reaper_lockrun|1
# v2.8.9 -- Service Intercept: nat POSTROUTING return-path chain + its card
sbin/rc|REAPER_FWM|1
www/Reaper_Firewall.asp|ic_tbl|1
# v2.9.0 -- the vendor factory-test hook is REMOVED and must never come back:
# it ran `sh /tmp/mnt/sda1/auto_test.sh` as root every 30 s when nvram ci=1.
sbin/rc|!auto_test.sh
# v2.9.4 regression markers - each pins a fix so a later change cannot silently
# drop it: the QoS drops-interpretation note, the Firewall Status forwards +
# intercepts rows, the Devices export reserved-IP columns, the rail-clock
# re-sync helper (and its carrier attribute on the banner fragment), and the
# Smart Connect migrated-state hint swap.
www/Reaper_QoS.asp|rqos_dropnote|1
www/Reaper_Firewall.asp|nicpt|1
www/Reaper_Devices.asp|reserved_ip|1
www/js/reaper_clock.js|visibilitychange|1
www/Reaper_SecBanner.asp|data-now|1
www/Advanced_Smart_Connect.asp|scWhereHint|1
# v2.9.6 -- full-backup restore drains the TLS-buffered upload tail (the one
# missing fputc that made every metal restore from v2.7.2 to v2.9.4 answer
# "magic"); the card's message/pending divs are un-hidden with an explicit
# display="block" (the stylesheet rule beat style.display=""); every refusal
# reaches syslog; the stock restore tail stamps each step. Missing = the
# restore silently fails again with no trace anywhere.
usr/sbin/httpd|upload tail drained from the TLS/stdio buffer|1
usr/sbin/httpd|full-backup restore REFUSED|1
www/Reaper_Backup.asp|'block'|1
www/Reaper_Backup.asp|!style.display=""
usr/lib/libwebapi.so|settings restore: nvram committed|1
# v2.9.8 -- native Backup & Restore page (Reaper_Backup.asp) replaces the
# stock Restore/Save/Upload page (menu repointed, stock page SUP-redirected)
# and absorbs the injected card; the pending-restore banner rides the shell
# and the dashboard so Complete restore is one click from the landing page.
# Missing = the restore flow regresses to the injected-card era.
# v3.0.6 -- the full backup carries the whole /jffs partition (fourth .rbk
# part; a restore that has it replaces /jffs and parks no pending half) and
# the settings JSON embeds the stock .CFG (cfg_b64; same-model import goes
# through reaper_restore.cgi). The stock .CFG/JFFS cards are gone from the
# page. Missing = the archive silently drops back to settings-only.
www/Reaper_Backup.asp|cfg_b64|1
usr/sbin/httpd|/jffs restored from the archive|1
usr/sbin/httpd|"cfg_b64":|1
www/reaper_shell.asp|import_pending|1
www/Main_ReaperDash.asp|import_pending|1
www/require/modules/menuTree.js|Reaper_Backup.asp|1
usr/sbin/httpd|Reaper_Backup.asp|1
# v3.0.1 -- the watchdog's self-heal runner is dispatchable again. `rc` selects an
# applet by argv[0] BASENAME only: the argv shift for an `rc <applet>` invocation
# is #if 0 in rc.c, and DEBUG_RCTEST maps a bare "rc" to rctest_main -- so
# `rc reaper_lockrun <script>` fell through and did nothing. From v2.7.3 to
# v3.0.0 EVERY rwatch self-heal (policy routing AND the Warden CRITICAL heal)
# logged that it was re-applying and re-applied nothing. Line 1 proves the
# symlink ships -- a marker fails when its own file is missing, which is the only
# way this format can assert a file exists. Line 2 blocks a regression to the
# dead form. Both verified RED against the v3.0.0 staged fs (3 hits of the dead
# form, no symlink). The `rc reaper_lockrun` phrase also appears in a source
# COMMENT in rwatch.c -- comments never reach .rodata, so line 2 stays clean.
sbin/reaper_lockrun|reaper_lockrun|1
sbin/rc|!rc reaper_lockrun
# v3.0.1 -- the ip-rule heal owns its counter, throttle and give-up latch. It
# shared heal.n/heal.at with the mark heal, and the mark block cleared the
# counter on every tick whenever the mangle chain was healthy -- which is the
# exact field state reported -- so the five-strike cap was unreachable and the
# heal re-ran every 5 minutes forever. Verified RED against v3.0.0 (0 hits).
sbin/rc|heal.rn|1
sbin/rc|heal.rat|1
sbin/rc|heal.rgaveup|1
# v3.0.1 -- both /proc/blog/skip_wireguard_network writes carry a TRAILING SPACE.
# The kernel handler copy_from_user()s into a 128-byte stack buffer, never
# NUL-terminates it, then runs strsep(" ")/strlen over it and breaks once it has
# the ip token. The trailing space puts strsep's NUL inside the bytes we wrote,
# so the ip token is always terminated in-region. Dropping it (or reverting the
# del site to `echo`, which appends a newline instead) puts the parse back over
# uninitialised kernel stack on every WireGuard-target apply AND revert.
# NOTE the DOUBLED PERCENT. These lines are generated shell held in a C fprintf
# FORMAT string, so what lands in sbin/rc's .rodata is the format -- `%%s` --
# not the `%s` the generated script ends up carrying at runtime. Matching the
# runtime form gives x0 and fails the build (it did, on the first v3.0.1 build).
# Same trap for any marker pinning shell that rc/httpd emit through *printf.
sbin/rc|printf 'add %%s ' |1
sbin/rc|printf 'del %%s ' |1
# v3.0.5 -- PBR/Warden Rung B (F6-F11). R6: the commit-confirm revert checks
# its own result and says so (REVERT FAILED, E_REVERT), and its rules script is
# on reaper_lockrun's allowlist so it runs under the firewall lock. rc reports
# refusals/failures to the page as CODES (E_*), never prose.
sbin/rc|REVERT FAILED|1
sbin/rc|/tmp/reaper_pbr/rollback.sh.rules|1
sbin/rc|E_NOSNAP|1
sbin/rc|E_SAVE|1
# R7: the page polls the whole commit-confirm window (not 12 x 700 ms - the
# negative pins the old budget) and every Keep / Revert acknowledges its
# response through pbrAck(); the partial-load code is handled, not shown raw.
www/Reaper_VPNRouting.asp|!tries<12
www/Reaper_VPNRouting.asp|pbrAck(|1
www/Reaper_VPNRouting.asp|E_PARTIAL|1
# v3.0.5 -- /tmp/resolv.dnsmasq is dnsmasq's SERVERS-FILE (`server=` lines).
# Parsing it for `nameserver` lines is the v2.6.2 Warden self-filter bug (the
# resolver carve-out exempted nothing) and the diag misread ("dnsmasq not
# forwarding upstream" on every report) coming back. Both negatives.
sbin/rc|!nameserver/{print $2}' /tmp/resolv.dnsmasq
usr/sbin/reaper_diag|!nameserver/{print $2}' /tmp/resolv.dnsmasq
# v1.3.13 -- rmcpd reported as arm-gated, not as a dead switch-driven service.
usr/sbin/reaper_diag|arm-gated|1
# v3.0.5 -- the 10 Gbit/s speed-test reports. The engine is launched through
# /usr/sbin/ookla_run (stderr KEPT per run, nice -10, argv passed through); a
# build that drops the wrapper silently goes back to discarding the engine's
# errors. Line 1 proves the wrapper ships (file-exists assertion + its exec
# target), line 2 that httpd calls it, line 3 that the diag reads what it keeps.
usr/sbin/ookla_run|nice -n -10 ookla|1
usr/sbin/ookla_run|lastfail|1
usr/sbin/httpd|ookla_run|1
usr/sbin/reaper_diag|10b. INTERNET SPEED TEST|1
